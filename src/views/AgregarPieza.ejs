<%- include ("./partials/_header") %>

    <div class="container mt-5">

        <div class="row">
            <!-- FORMULARIO-->
            <div class="col-md-5">
                <div class="card">
                    <h1 class="subtitle mb-0">Agregar Pieza</h1>
                    <div class="card-body">
                        <form action="/addPieza/" method="post">
                            <input class="form-control" style="display: none;" type="text" name="planta" value="<%=usu[0].planta%>" required />
                            <div class="form-group">
                                <label for="nombrepieza">Pieza:</label>
                                <input id="nombrepieza" class="form-control" type="text" name="nombre" placeholder="Nombre de Pieza">
                            </div>
                            <div class="form-group row text-center">
                                <div class="col">
                                    <button class="myButton" type="submit">Agregar</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

            </div>

            <!--  TABLA CON LOS DATOS-->

            <div class="col-md-7 mt-md-0 mt-5 mb-5">
                <table class="table table-bordered table-striped table-hover">
                    <thead class="text-white tabla">
                        <tr>
                            <th>#</th>
                            <th>Pieza</th>
                            <th>Opción</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% var cont = 0; %>
                            <% for (var i = tasks.length-1; i >= 0; i--)  {     %>
                                <tr>
                                    <td class="align-middle">
                                        <%= cont+1 %>
                                    </td>
                                    <td class="align-middle"> <strong> <%= tasks[i].nombre %>   </strong> </td>
                                    <td class="align-middle">
                                        <a href=/deletePieza/<%=tasks[i]._id %>   class="btn btn-danger" href="turn">Eliminar </a>
                                    </td>
                                </tr>
                                <%  cont++;}  %>
                                    <!--  TABLA CON LOS DATOS-->
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script src="/js/validate.js"></script>

    <%- include ("./partials/_footer") %>